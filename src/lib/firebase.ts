// Firebase stub — actual firebase SDK is optional and loaded only when configured
// The app uses Supabase for authentication. Firebase is not required.

export const auth: any = null;
export const googleProvider: any = null;
export const getFirebaseAuth = () => null;
export const getGoogleProvider = () => null;
export const getFirebaseApp = () => null;

export default null;
